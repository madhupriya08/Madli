Used to define ranking vocabulary — score, window, locals — never to hold copy the user needs.

```jsx
<Tooltip label="Locals are people whose last 20 check-ins are within 5km."><Icon name="info" size={14} /></Tooltip>
```
