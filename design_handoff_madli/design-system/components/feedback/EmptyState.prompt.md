Direct, one apology at most, always an exit.

```jsx
<EmptyState title="No ranking here yet" body="We need about 50 local ratings before we will call anything a pick." action={<Button variant="secondary">Try a nearby area</Button>} />
```
