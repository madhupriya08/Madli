Titles are set in Inter SemiBold, not Cooper — Cooper is for the content headline below the bar.

```jsx
<TopBar title="Kadıköy" subtitle="3 picks · updated 2h ago" trailing={<IconButton icon="sliders-horizontal" label="Filters" />} />
```
