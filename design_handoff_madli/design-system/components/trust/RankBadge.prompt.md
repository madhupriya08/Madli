Marks a pick's position. Madli only ever shows three, so ranks above 3 are a bug.

```jsx
<RankBadge rank={1} size="md" />
```

Colour comes from `--rank-1` (teal), `--rank-2` (sky), `--rank-3` (slate). Use `outline` on cream or photographic backgrounds where a solid disc would fight the image.
