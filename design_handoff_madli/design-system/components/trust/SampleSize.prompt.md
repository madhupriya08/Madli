Always exact numbers. "412 locals" is on-brand; "thousands of reviews" is not.

```jsx
<SampleSize locals={412} visitors={88} window="last 90 days" />
```

Renders as a middot-separated 11px grey line. If the count is low, pair with `<RankGap tone="thin" />`.
