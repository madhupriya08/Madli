Mutually exclusive options — ranking basis, trip type.

```jsx
<Radio name="basis" label="Locals only" description="Ignores visitor ratings" checked={basis === "locals"} onChange={() => setBasis("locals")} />
```
