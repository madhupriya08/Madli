The one pill-shaped field in the system; used for city / craving search at the top of the app and the marketing hero.

```jsx
<SearchField size="lg" value={q} onChange={e => setQ(e.target.value)} placeholder="Where are you?" />
```
