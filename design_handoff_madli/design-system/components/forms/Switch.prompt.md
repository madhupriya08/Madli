For instant-effect settings only. Anything that needs saving uses Checkbox plus a Save button.

```jsx
<Switch label="Show ranking gaps" description="Tell me when #1 and #2 are close" checked={gaps} onChange={setGaps} />
```
