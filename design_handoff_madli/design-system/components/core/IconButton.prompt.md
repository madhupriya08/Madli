For controls with no room for a label — save, share, close, back.

```jsx
<IconButton icon="bookmark" label="Save pick" variant="onImage" />
```

`onImage` is the only variant allowed on top of photography (translucent white capsule, teal ink). Always pass `label`.
