Generic surface for anything that is not a pick — settings rows, evidence panels, marketing blocks.

```jsx
<Card padding="var(--space-5)" interactive>…</Card>
```

For a ranked pick use `PickCard` instead; it carries the reason and the evidence line.
